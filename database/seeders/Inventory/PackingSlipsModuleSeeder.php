<?php

namespace Database\Seeders\Inventory;

use Illuminate\Database\Seeder;

/**
 * Seed data for Inventory → Packing slips.
 *
 * Packing slips are generated from deliveries, so this seeder
 * is currently a placeholder. When we add Sales demo deliveries,
 * we can extend this to create example packing slip records.
 */
class PackingSlipsModuleSeeder extends Seeder
{
    public function run(): void
    {
        // Intentionally left empty for now.
    }
}

